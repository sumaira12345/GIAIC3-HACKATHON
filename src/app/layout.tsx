import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";
import { CartProvider } from "./cart/context/CartContext";
import Footer from "./components/HomeComponent/footer";
import Header from "./components/HomeComponent/header";
import TopHeader from "./components/HomeComponent/TopHeader";
import { WishlistProvider } from "./wishlist/ContextWishlist/WishlistContext";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Hackathon-ecommerce 24",
  description: "Generated by create Mubashir Saeedi",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <CartProvider>
          <WishlistProvider>
            <TopHeader />
            <Header />
            {children}
          </WishlistProvider>
          <Footer />
        </CartProvider>
      </body>
    </html>
  );
}
